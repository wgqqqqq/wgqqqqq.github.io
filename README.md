# Guanqi | 本地 agent 开发基础设施

这个仓库是个人 GitHub Pages 站点。默认中文，英文版在 `/en/`。主页聚焦本地 agent 开发基础设施、BitFun 和 flashgrep。

本地构建：

```bash
bundle install --path vendor/bundle
bundle exec jekyll serve
```

主页入口在 `index.html` 和 `en/index.html`，核心布局在 `_layouts/blog.html`。关于页在 `about.html` 和 `en/about.html`。
